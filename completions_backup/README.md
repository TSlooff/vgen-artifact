# Completions Back-up
This directory contains the back-up files of the completions for which markdown was removed. The stats.csv file contains basic information about which files were replaced, as well as the edit distance between the original and the stripped file.
