// This module implements the 61-bit rotate function in the Present keyschedule
module rotate_61(
    input [79:0] In,
    output [79:0] R
    ); 
// In is the 80-bit input vector
// R is the 80-bit output vector which is In rotated left by 61 bits 
// R = In <<< 61
